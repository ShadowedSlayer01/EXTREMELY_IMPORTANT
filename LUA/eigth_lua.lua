--[[Tables which are hypothetically arrays can contain  values]]
-- table.sort sorts a table in alphabetical order by default
-- table.insert is to add an element at a given index
-- table.remove is to remove an element at a given index
-- table.concat concatenates all the ellement of a table take a seperator

local arr = {
    "lol", 1000, 5.651, true, "Ops",
}
-- You can make tables inside tables
local dub_arr = {
    {126, "JFK", 1997}, {"jkg", 2025, 9999}, {123, 564, 999}
}